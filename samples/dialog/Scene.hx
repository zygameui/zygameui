package dialog;

import zygame.components.ZBuilderScene;

@:build(zygame.macro.AutoBuilder.build("Adventure"))
class Scene extends ZBuilderScene {

    override function onBuilded() {
        super.onBuilded();
    }


}